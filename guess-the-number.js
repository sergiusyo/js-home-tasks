// Угадай число.
// Программа загадывает число от 1 до 100. Пользователь угадывает. При каждой попытке выводите
// «Больше» или «Меньше». Когда угадал — «Верно! Попыток: N».

// (с подробным описанием)

const secret = Math.floor(Math.random() * 100) + 1; // генерация секретного числа.
// Math.random() - Это функция, возвращающая случайное число от 0 (включительно) до 1 (не включительно).
// (0.42, 0.007, 0.999, 0.123456).
// Math.random() * 100 - Умножаем случайное число на 100, получаем число от 0 до 99.999...
// 42.3, 0.7, 99.9, 12.34.
// Math.floor(Math.random() * 100)
// Math.floor() — округляет число вниз до целого
// Теперь получаем целое число от 0 до 99
// 42, 0, 99, 12

// Math.floor(Math.random() * 100) + 1
// Прибавляем 1
// Теперь получаем число от 1 до 100
// 43, 1, 100, 13

// Пример. Math.floor(0.52 * 52.73) + 1

// Итог: secret — это случайное целое число от 1 до 100.

// let guess = Number(prompt("Угадай число (1-100):"));
// Итог: guess — это число, которое ввёл пользователь (преобразованная строка).

// let attempts = 1;
// Создаётся переменная attempts (количество попыток)
// Начальное значение 1 (первая попытка уже сделана)

// while (guess !== secret) {
// пока предположение НЕ равно секретному числу.
// Пример: если secret = 42, а guess = 50, то 50 !== 42 → true → входим в цикл.

//   if (guess < secret) console.log("Больше!");
//   else console.log("Меньше!");
// Логика:
// Если guess < secret (предположение меньше загаданного числа) → выводим "Больше!" (надо больше)
// Иначе (guess > secret) → выводим "Меньше!" (надо меньше)
// Пример для secret = 42, guess = 50:
// 50 < 42? → false → переходим к else
// Вывод: "Меньше!"(потому что 50 больше, чем 42)

//   guess = Number(prompt("Ещё раз:"));
// Снова просим пользователя ввести число
// Сохраняем новое предположение в guess

//   attempts++; // Увеличиваем счётчик попыток на 1
// }
// console.log(`Верно! Попыток: ${attempts}`);

// Когда пользователь угадал число (guess === secret), цикл заканчивается
// Выводится поздравление и количество попыток
