// var reverseWords = function (s) {
//   // 1. Переворачиваем всю строку целиком
//   const reversedString = s.split("").reverse().join("");

//   // 2. Разбиваем перевернутую строку на слова, переворачиваем их порядок и собираем обратно
//   const result = reversedString.split(" ").reverse().join(" ");
//   return result;
// };

// console.log(reverseWords("Let's take LeetCode contest"));
